spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/test-example
    username: postgres
    password: password
  liquibase:
    change-log: classpath:/db/changelog/db.changelog-master.yaml
    drop-first: true